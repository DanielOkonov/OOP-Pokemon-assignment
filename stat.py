class Stat:
    def __init__(self, name, stat_id, is_battle_only, base_stat):
        self.name = name
        self.stat_id = stat_id
        self.is_battle_only = is_battle_only
        self.base_stat = base_stat  # Base value of the stat

    # Additional methods for stat-related functionality
